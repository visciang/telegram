name: CI

on:
  push:
    tags:
      - "*"
    branches:
      - master
  pull_request:

jobs:
  build:
    uses: visciang/gha-elixir-workflow/.github/workflows/elixir.yml@dev
    secrets:
      github-token: ${{ secrets.GITHUB_TOKEN }}

