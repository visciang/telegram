# Used by "mix format" and to export configuration.
[
  line_length: 120,
  inputs: [
    "example/**/*.{ex,exs}",
    "lib/**/*.{ex,exs}",
    "test/**/*.{ex,exs}",
    "mix.exs"
  ],
  import_deps: [:tesla],
]
